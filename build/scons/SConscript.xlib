Import("env")
Import("libutil")

b = "build_xlib"

localEnv = env.Clone()

localEnv.VariantDir(b, "../../xlib", duplicate = 0)
xlib = localEnv.Program(b + "/xlib", LIBS = [libutil, "m"], source = [
	b + "/library.c",
	b + "/main.c"
	])

Return("xlib")
