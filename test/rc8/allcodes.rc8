		SECTION "Test",CODE
		
		nop

		ld	(bc),t
		ld	(de),t
		ld	(hl),t

		ld	t,(ft)
		ld	t,(bc)
		ld	t,(de)
		ld	t,(hl)

		not	f
		not	t
		DB	$0A
		DB	$0B
		lco	t,(ft)
		lco	t,(bc)
		lco	t,(de)
		lco	t,(hl)

		ld	t,(bc+f)
		ld	t,(bc+t)
		ei
		di
		ld	t,(bc+d)
		ld	t,(bc+e)
		ld	t,(bc+h)
		ld	t,(bc+l)

		ld	(bc+f),t
		sys	$19
		DB	$1A
		DB	$1B
		ld	(bc+d),t
		ld	(bc+e),t
		ld	(bc+h),t
		ld	(bc+l),t

		DB	$20
		DB	$21
		DB	$22
		DB	$23
		DB	$24
		DB	$25
		DB	$26
		DB	$27

		DB	$28
		DB	$29
		DB	$2A
		DB	$2B
		DB	$2C
		DB	$2D
		DB	$2E
		DB	$2F

		DB	$30
		DB	$31
		DB	$32
		DB	$33
		DB	$34
		DB	$35
		DB	$36
		DB	$37

		push	ft
		push	bc
		push	de
		push	hl

		pop	ft
		pop	bc
		pop	de
		pop	hl

		add	t,f
		add	t,t
		add	t,b
		add	t,c
		add	d
		add	e
		add	h
		add	l

		inc	f
		inc	t
		inc	b
		inc	c
		inc	d
		inc	e
		inc	h
		inc	l

		sub	t,f
		neg	t
		sub	t,b
		sub	c
		sub	d
		sub	e
		sub	h
		sub	l

		dec	f
		dec	t
		dec	b
		dec	c
		dec	d
		dec	e
		dec	h
		dec	l

		ld	f,t
		DB	$61
		ld	b,t
		ld	c,t
		ld	d,t
		ld	e,t
		ld	h,t
		ld	l,t

		ld	t,f
		reti
		ld	t,b
		ld	t,c
		ld	t,d
		ld	t,e
		ld	t,h
		ld	t,l

		ld	f,$70
		ld	t,$71
		ld	b,$72
		ld	c,$73
		ld	d,$74
		ld	e,$75
		ld	h,$76
		ld	l,$77

		cmp	f
		ext
		cmp	b
		cmp	c
		cmp	d
		cmp	e
		cmp	h
		cmp	l

		j/le	.end
		j/gt	.end
		j/lt	.end
		j/ge	.end
		j/leu	.end
		j/gtu	.end
		j/ltu	.end
		j/geu	.end

		j/eq	.end
		j/ne	.end
		j	.end
		DB	$8B
		DB	$8C
		DB	$8D
		DB	$8E
		DB	$8F
.end		
		dj	f,.here
		dj	t,.here
		dj	b,.here
		dj	c,.here
		dj	d,.here
		dj	e,.here
		dj	h,.here
		dj	l,.here
.here

		jal	(ft)
		jal	(bc)
		jal	(de)
		jal	(hl)
		j	(ft)
		j	(bc)
		j	(de)
		j	(hl)

		tst	f
		tst	t
		tst	b
		tst	c
		tst	d
		tst	e
		tst	h
		tst	l

		or	t,f
		DB	$A9
		or	t,b
		or	t,c
		or	d
		or	e
		or	h
		or	l

		and	t,f
		DB	$B1
		and	t,b
		and	t,c
		and	d
		and	e
		and	h
		and	l

		xor	t,f
		DB	$B9
		xor	t,b
		xor	t,c
		xor	d
		xor	e
		xor	h
		xor	l

		inc	ft
		inc	bc
		inc	de
		inc	hl

		dec	ft
		dec	bc
		dec	de
		dec	hl

		swap	ft
		swap	bc
		swap	de
		swap	hl

		lcr	(c),t

		cmp	bc
		cmp	de
		cmp	hl

		lio	(bc),t

		ld	bc,ft
		ld	de,ft
		ld	hl,ft

		tst	ft
		tst	bc
		tst	de
		tst	hl

		lio	t,(bc)

		ld	ft,bc
		ld	ft,de
		ld	ft,hl

		lcr	t,(c)

		DB	$DD
		DB	$DE
		DB	$DF

		DB	$E0
		DB	$E1

		ls	ft,b
		ls	c
		ls	ft,d
		ls	ft,e
		ls	ft,h
		ls	ft,l

		DB	$E8
		DB	$E9

		rs	ft,b
		rs	c
		rs	ft,d
		rs	ft,e
		rs	ft,h
		rs	ft,l

		neg	ft

		sub	bc
		sub	ft,de
		sub	ft,hl

		add	ft,ft
		add	bc
		add	ft,de
		add	ft,hl

		pusha
		popa

		rsa	ft,b
		rsa	c
		rsa	ft,d
		rsa	ft,e
		rsa	ft,h
		rsa	ft,l
